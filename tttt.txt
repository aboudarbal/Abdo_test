Suite à notre réunion ce matin, veuillez trouver en pièce jointe les scripts de qualification discutés.

De notre côté, il n'est pas envisageable de modifier les scripts sur tous les environnements, cela relève de votre responsabilité.

À titre informatif, le changement devra être effectué après le 05/02, en raison du gel de fin du mois.

Restant à votre disposition pour toute clarification nécessaire.

Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -eq 1521 }



sudo find /etc/ssl/certs -type f -exec openssl x509 -in {} -outform der | openssl base64 -A -w 0 \;
